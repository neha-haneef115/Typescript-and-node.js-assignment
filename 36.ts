function make_shirt(size: string, message: string): void {
    console.log(`You have ordered a ${size} T-shirt with message.${message}.`);
}

make_shirt("medium- seized","Be stubborn or admit you are Dumb");
