let famousperson: string="Confucius";
let quote: string="It doesn't matter how slow you go as long as you do not stop";
let message: string= `${famousperson} once said:"${quote}"`;
console.log(message);